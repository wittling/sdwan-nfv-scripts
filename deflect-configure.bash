#!/bin/bash

logger "CONFIGURATION of the Deflect"

logger "Hostname: ${hostname}"
logger "Traffic IP Address: ${dfl-net}" 
logger "Traffic Interface: ${trafficiface}" 
logger "DSX IP Address: ${dsxprimary_dsx-net}"
